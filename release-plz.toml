[workspace]
changelog_update = false
git_tag_enable = false
git_release_enable = false
semver_check = false
pr_body = """
Changelog and release notes are generated by git-cliff (see `cliff.toml`).
The `CHANGELOG.md` and PR body are populated by a follow-up CI step.
"""

[[package]]
name = "lisette"
git_tag_enable = true
git_release_enable = true
changelog_update = false
