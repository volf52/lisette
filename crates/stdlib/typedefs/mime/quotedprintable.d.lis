// Generated by Lisette bindgen
// Source: mime/quotedprintable (Go stdlib)
// Go: 1.26.7
// Lisette: 0.12.1

import "go:io"

/// NewReader returns a quoted-printable reader, decoding from r.
pub fn NewReader(r: io.Reader) -> mut Ref<Reader>

/// NewWriter returns a new [Writer] that writes to w.
pub fn NewWriter(w: io.Writer) -> mut Ref<Writer>

/// Reader is a quoted-printable decoder.
pub type Reader

/// A Writer is a quoted-printable writer that implements [io.WriteCloser].
#[go(hidden_fields)]
#[go(zero_unsafe)]
pub struct Writer {
  pub Binary: bool,
}

impl Reader {
  /// Read reads and decodes quoted-printable data from the underlying reader.
  fn Read(self: mut Ref<Reader>, p: mut Slice<byte>) -> Partial<int, error>
}

impl Writer {
  /// Close closes the [Writer], flushing any unwritten data to the underlying
  /// [io.Writer], but does not close the underlying io.Writer.
  #[allow(unused_result)]
  fn Close(self: mut Ref<Writer>) -> Result<(), error>

  /// Write encodes p using quoted-printable encoding and writes it to the
  /// underlying [io.Writer]. It limits line length to 76 characters. The encoded
  /// bytes are not necessarily flushed until the [Writer] is closed.
  fn Write(self: mut Ref<Writer>, p: Slice<byte>) -> Partial<int, error>
}
