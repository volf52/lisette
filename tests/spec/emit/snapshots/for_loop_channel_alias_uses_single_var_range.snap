---
source: tests/spec/emit/control_flow.rs
description: |
  
  type Inbox = Channel<int>
  
  fn drain(ch: Inbox) -> int {
    let mut total = 0
    for value in ch {
      total = total + value
    }
    total
  }
---
package main

import lisette "github.com/ivov/lisette/prelude"

type Inbox = chan int

func drain(ch Inbox) int {
	total := 0
	for value := range lisette.ChannelRange(ch) {
		total += value
	}
	return total
}
