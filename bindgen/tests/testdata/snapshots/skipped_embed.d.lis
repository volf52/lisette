// Generated by Lisette bindgen
// Source: ./testdata/fixtures/skipped_embed (local)
// Go: 0.0.0
// Lisette: 0.0.0

pub struct Box<T> {
  pub V: T,
}

/// Embeds Box[struct{...}] with a tagged anonymous struct type argument, which is
/// unrepresentable, so bindgen skips the embed; Host.M must stay flattened, not
/// dropped with it. Distinct from Widget above: the gate propagates through a
/// generic type argument, not a direct internal-package embed.
#[go(hidden_embed)]
pub struct Host {
  // SKIPPED field "Box": anonymous-struct — anonymous struct types are not supported
  pub Y: int,
}

/// Exported embed of an internal-package type: bindgen skips the field, so Widget
/// looks flat but must stay unembeddable (marked) with hidden.Engine.Run kept.
#[go(hidden_embed)]
pub struct Widget {
  // SKIPPED field "Engine": internal-package-ref — references type from internal package "github.com/ivov/lisette/bindgen/tests/testdata/fixtures/skipped_embed/internal/hidden"
  pub X: int,
}

impl<T> Box<T> {
  fn M(self) -> int
}

impl Host {
  fn M(self) -> int
}

impl Widget {
  fn Run(self) -> int
}
