// Generated by Lisette bindgen
// Source: crypto/x509/pkix (Go stdlib)
// Go: 1.26.7
// Lisette: 0.12.1

import "go:encoding/asn1"
import "go:math/big"
import "go:time"

/// AlgorithmIdentifier represents the ASN.1 structure of the same name. See RFC
/// 5280, section 4.1.1.2.
pub struct AlgorithmIdentifier {
  pub Algorithm: mut asn1.ObjectIdentifier,
  pub Parameters: mut asn1.RawValue,
}

/// AttributeTypeAndValue mirrors the ASN.1 structure of the same name in
/// RFC 5280, Section 4.1.2.4.
pub struct AttributeTypeAndValue {
  pub Type: mut asn1.ObjectIdentifier,
  pub Value: Unknown,
}

/// AttributeTypeAndValueSET represents a set of ASN.1 sequences of
/// [AttributeTypeAndValue] sequences from RFC 2986 (PKCS #10).
pub struct AttributeTypeAndValueSET {
  pub Type: mut asn1.ObjectIdentifier,
  pub Value: mut Slice<mut Slice<mut AttributeTypeAndValue>>,
}

/// CertificateList represents the ASN.1 structure of the same name. See RFC
/// 5280, section 5.1. Use Certificate.CheckCRLSignature to verify the
/// signature.
/// 
/// Deprecated: x509.RevocationList should be used instead.
pub struct CertificateList {
  pub TBSCertList: mut TBSCertificateList,
  pub SignatureAlgorithm: mut AlgorithmIdentifier,
  pub SignatureValue: mut asn1.BitString,
}

/// Extension represents the ASN.1 structure of the same name. See RFC
/// 5280, section 4.2.
pub struct Extension {
  pub Id: mut asn1.ObjectIdentifier,
  pub Critical: bool,
  pub Value: mut Slice<byte>,
}

/// Name represents an X.509 distinguished name. This only includes the common
/// elements of a DN. Note that Name is only an approximation of the X.509
/// structure. If an accurate representation is needed, asn1.Unmarshal the raw
/// subject or issuer as an [RDNSequence].
pub struct Name {
  pub Country: mut Slice<string>,
  pub Organization: mut Slice<string>,
  pub OrganizationalUnit: mut Slice<string>,
  pub Locality: mut Slice<string>,
  pub Province: mut Slice<string>,
  pub StreetAddress: mut Slice<string>,
  pub PostalCode: mut Slice<string>,
  pub SerialNumber: string,
  pub CommonName: string,
  pub Names: mut Slice<mut AttributeTypeAndValue>,
  pub ExtraNames: mut Slice<mut AttributeTypeAndValue>,
}

pub struct RDNSequence(mut Slice<mut RelativeDistinguishedNameSET>)

pub struct RelativeDistinguishedNameSET(mut Slice<mut AttributeTypeAndValue>)

/// RevokedCertificate represents the ASN.1 structure of the same name. See RFC
/// 5280, section 5.1.
pub struct RevokedCertificate {
  pub SerialNumber: Option<mut Ref<big.Int>>,
  pub RevocationTime: mut time.Time,
  pub Extensions: mut Slice<mut Extension>,
}

/// TBSCertificateList represents the ASN.1 structure of the same name. See RFC
/// 5280, section 5.1.
/// 
/// Deprecated: x509.RevocationList should be used instead.
pub struct TBSCertificateList {
  pub Raw: mut asn1.RawContent,
  pub Version: int,
  pub Signature: mut AlgorithmIdentifier,
  pub Issuer: mut RDNSequence,
  pub ThisUpdate: mut time.Time,
  pub NextUpdate: mut time.Time,
  pub RevokedCertificates: mut Slice<mut RevokedCertificate>,
  pub Extensions: mut Slice<mut Extension>,
}

impl CertificateList {
  /// HasExpired reports whether certList should have been updated by now.
  fn HasExpired(self: Ref<CertificateList>, now: time.Time) -> bool
}

impl Name {
  /// FillFromRDNSequence populates n from the provided [RDNSequence].
  /// Multi-entry RDNs are flattened, all entries are added to the
  /// relevant n fields, and the grouping is not preserved.
  fn FillFromRDNSequence(self: mut Ref<Name>, rdns: Ref<RDNSequence>)

  /// String returns the string form of n, roughly following
  /// the RFC 2253 Distinguished Names syntax.
  fn String(self) -> string

  /// ToRDNSequence converts n into a single [RDNSequence]. The following
  /// attributes are encoded as multi-value RDNs:
  /// 
  ///   - Country
  ///   - Organization
  ///   - OrganizationalUnit
  ///   - Locality
  ///   - Province
  ///   - StreetAddress
  ///   - PostalCode
  /// 
  /// Each ExtraNames entry is encoded as an individual RDN.
  fn ToRDNSequence(self) -> RDNSequence
}

impl RDNSequence {
  /// String returns a string representation of the sequence r,
  /// roughly following the RFC 2253 Distinguished Names syntax.
  fn String(self) -> string
}
